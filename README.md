# search-engine-scraper

A simple scraper that uses **Cuimp + Cheerio** for non-JS sites and **Patchright** for sites that need browser automation.

## Installation / Setup

```bash
git clone https://github.com/zewhal/search-engine-scraper
cd search-engine-scraper
bun install